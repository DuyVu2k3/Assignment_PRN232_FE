import { PlaceholderShell } from "./PlaceholderShell";

export function AssignExaminerByBatchPage() {
  return (
    <PlaceholderShell
      title="Gán examiner theo batch"
      description="Phân công examiner theo batch của một kỳ thi (Exam)."
      primaryActionLabel="Áp dụng phân công"
      apiHint="Assignment theo Batch — batch + examiner."
    />
  );
}
