import { PlaceholderShell } from "./PlaceholderShell";

export function ScoreReportsPage() {
  return (
    <PlaceholderShell
      title="Báo cáo & tổng hợp điểm"
      description="Tổng hợp điểm theo đề, học kỳ hoặc batch."
      primaryActionLabel="Xuất báo cáo"
      apiHint="Reports / aggregates — export CSV/PDF khi có API."
    />
  );
}
