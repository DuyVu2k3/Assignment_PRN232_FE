import { PlaceholderShell } from "./PlaceholderShell";

export function ModeratorBatchTrackingPage() {
  return (
    <PlaceholderShell
      title="Theo dõi xử lý batch"
      description="Trạng thái batch do bạn upload (Moderator)."
      apiHint="Polling hoặc signalR theo batchId — tùy backend."
    />
  );
}
