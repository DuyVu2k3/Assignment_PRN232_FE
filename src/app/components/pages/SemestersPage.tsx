import { PlaceholderShell } from "./PlaceholderShell";

export function SemestersPage() {
  return (
    <PlaceholderShell
      title="Học kỳ"
      description="Quản lý học kỳ (Semesters)."
      primaryActionLabel="Thêm học kỳ"
      apiHint="CRUD Semesters — gắn endpoint backend khi sẵn sàng."
    />
  );
}
