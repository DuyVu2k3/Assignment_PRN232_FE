import { PlaceholderShell } from "./PlaceholderShell";

export function GradingHistoryPage() {
  return (
    <PlaceholderShell
      title="Lịch sử chấm"
      description="Xem lại điểm và nhận xét đã lưu."
      apiHint="Lịch sử chấm theo examiner — grades / rubric scores."
    />
  );
}
